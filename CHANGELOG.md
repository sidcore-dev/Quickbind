# Changelog

All notable changes to this project are documented here.
Versions correspond to the `version` field in `fxmanifest.lua`.

## [1.0.0] - 2026-08-02

### Added

- Initial release: binds a configurable hotkey (default F6) to a configurable chat command (default `/vs`)
